{
	"name": "g1-mcp-ads-client",
	"version": "0.1.0",
	"private": true,
	"description": "MCP Ads format SDK suite — JS/TS (Phase 5b live) + Python (Phase 6 GA stub) + Go (Phase 6 GA stub). OSS MIT. Implements specs/mcp-ads-v1.0.md.",
	"scripts": {
		"typecheck": "pnpm -r typecheck",
		"build": "pnpm -r build",
		"test": "pnpm -r test",
		"lint": "biome check .",
		"lint:fix": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"typescript": "^5.7.0"
	},
	"packageManager": "pnpm@10.33.0"
}
